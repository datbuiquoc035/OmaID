OmaID SDDM theme source

This directory is copied to a root-owned SDDM theme directory during installation. It must not be placed in the user's home because the greeter runs as the `sddm` user.

The theme is intentionally separate from the Quickshell session-lock plugin.
