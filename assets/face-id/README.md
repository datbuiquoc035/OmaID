Face artwork contract

Place the canonical artwork at:

    assets/face-id/face.svg

The UI expects a square or near-square image that can be clipped inside a rounded square. The QML layer supplies the idle, scanning, success, and failure styling so the initial implementation only needs one neutral face asset.
